# Versions below were verified by actually installing them and test-importing
# every module in this project (July 2026). langchain went through a v0 -> v1
# restructuring at some point, so pin floors rather than trusting older
# tutorials/blog posts, which often show now-outdated import paths.

langchain>=1.3.0
langchain-core>=1.4.0
langchain-community>=0.4.0        # SQLDatabaseToolkit + DuckDuckGoSearchRun live here.
                                   # This package is in "sunset" maintenance mode upstream
                                   # (still works, prints a DeprecationWarning) - if it's
                                   # ever removed, migrate to the standalone successor
                                   # packages per the LangChain migration guide.
langgraph>=1.2.0
langchain-anthropic>=1.4.0
langchain-openai>=1.3.0
langchain-huggingface>=1.2.0      # local, no-API-key embeddings for the RAG store
langchain-chroma>=1.1.0
langchain-text-splitters>=1.1.0
sentence-transformers>=3.0.0      # backing model for langchain-huggingface embeddings
chromadb>=0.5.0
pypdf>=4.0.0
ddgs>=9.0.0                       # DuckDuckGo search backend (the old
                                   # `duckduckgo-search` package was renamed;
                                   # langchain-community imports `ddgs`)
python-dotenv>=1.0.0
pydantic>=2.0.0
streamlit>=1.38.0                 # optional UI (app_streamlit.py)
